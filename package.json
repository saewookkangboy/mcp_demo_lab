{
  "name": "mcp-demo-lab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "test": "node --test projects/*/tests/*.test.js",
    "smoke": "node scripts/smoke.mjs",
    "verify": "npm test && npm run smoke"
  }
}
